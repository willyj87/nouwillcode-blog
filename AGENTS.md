# AI Agents

## Technical Description

<system-reminder>
      IMPORTANT: this context may or may not be relevant to your tasks. 
      You should not respond to this context unless it is highly relevant to your task.
</system-reminder>
The stack is nextjs project with typescript, tailwindcss, shadcn and Sanity CMS.
Sanity CMS is used for content management, while Next.js serves as the frontend framework. Tailwind CSS provides utility-first styling, and Shadcn is used for building UI components. The project is structured to facilitate easy content updates and a responsive user interface.

## Agent Rules

You MUST adhere to the following rules when writing or modifying code in this project:

1. **Language:** English must be used for everything (communication with the user, code comments, variable names, commit messages, etc.).
2. **React/Next.js Architecture:**
   - Use React Server Components (RSC) by default.
   - Only add the `"use client"` directive when strictly necessary for interactive components (e.g., using state, effects, or event listeners).
   - Use functional components and explicitly type your `Props` with TypeScript.
   - Standardize imports in the following order: `react` -> `next` -> external packages -> internal components -> `lib`/`utils`.
3. **Styling and UI (Tailwind & Shadcn):**
   - Systematically use the `cn()` utility (which combines `clsx` and `tailwind-merge`) provided by Shadcn to handle conditional Tailwind classes.
   - When you need a new base UI component, always generate it using the Shadcn CLI (e.g., `npx shadcn@latest add <component>`) instead of building it from scratch.
4. **Data Management (Sanity & GROQ):**
   - Centralize all GROQ queries in dedicated files (e.g., `src/sanity/lib/queries.ts`) rather than scattering them inline within components.
   - Strictly type the responses of your Sanity queries using the types automatically generated by Sanity TypeGen.

## Project Structure

```markdown
.
├── public/                 # Static assets (icons, images, logos)
│   ├── file.svg
│   ├── globe.svg
│   └── window.svg
├── src/                    # Application source code
│   ├── app/                # Next.js App Router (Routing and pages)
│   │   ├── studio/         # Sanity Studio route
│   │   │   └── [[...tool]]/
│   │   │       └── page.tsx # Sanity Studio mounting page
│   │   ├── globals.css     # Global stylesheets and Tailwind CSS setup
│   │   ├── layout.tsx      # Root HTML layout structure
│   │   └── page.tsx        # Main application entry point
│   ├── components/         # Reusable React components
│   │   ├── icons/          # Custom SVG brand and utility icons
│   │   │   └── brand-icons.tsx
│   │   ├── portable/       # Custom rendering components for Sanity Portable Text
│   │   │   ├── code-block.tsx     # Custom code block syntax highlighting component
│   │   │   └── portable-text.tsx  # Rich-text portable text renderer wrapper
│   │   └── ui/             # Core base UI components (Shadcn UI)
│   │       ├── avatar.tsx
│   │       ├── badge.tsx
│   │       ├── button.tsx
│   │       ├── card.tsx
│   │       └── separator.tsx
│   ├── lib/                # Shared helper functions and utility modules
│   │   ├── format.ts       # Utility functions for dates and string formatting
│   │   └── utils.ts        # Tailwind merge helper (cn utility)
│   └── sanity/             # Sanity.io CMS configuration and integration client
│       ├── lib/
│       │   ├── client.ts   # Configured Sanity client instance
│       │   ├── highlight.ts
│       │   ├── image.ts    # Image helper for generating Sanity asset URLs
│       │   ├── live.ts     # Live content preview configuration
│       │   └── queries.ts  # GROQ queries for fetching documents
│       ├── schemaTypes/    # CMS document types schemas
│       │   ├── author.ts
│       │   ├── blockContent.ts
│       │   ├── category.ts
│       │   ├── index.ts    # Schema entry point exporting all schemas
│       │   └── post.ts
│       ├── env.ts          # Environment variables helper for Sanity
│       ├── structure.ts    # Structure builder configuration for Sanity Studio
│       └── types.ts        # Auto-generated TypeScript types for Sanity schemas
├── AGENTS.md               # AI agent configuration, rules, and workspace context
├── CLAUDE.md               # IDE profile instruction file for styling and dev guidelines
├── README.md               # Main repository documentation
├── components.json         # Shadcn CLI configuration file
├── eslint.config.mjs       # ESLint configurations
├── next-env.d.ts           # Next.js TypeScript declarations
├── next.config.ts          # Custom Next.js configuration settings
├── package.json            # Node.js dependencies and scripts
├── pnpm-lock.yaml          # Pnpm lockfile for dependency management
├── postcss.config.mjs      # PostCSS configuration for Tailwind CSS compilation
├── sanity.cli.ts           # Sanity CLI configuration
├── sanity.config.ts        # Main Sanity Studio configuration
└── tsconfig.json           # TypeScript compilation configuration
```

## Project Goal

This project is about building a blog website with a content management system (CMS) using Sanity. The structure is designed to separate concerns, making it easier to manage content, UI components, and application logic.

## Command to run the project

```bash
pnpm install
pnpm dev
```
